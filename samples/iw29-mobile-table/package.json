{
    "name": "iw29-mobile-table",
    "version": "1.0.0",
    "description": "== Local Preview Execute `npm start`",
    "main": "webapp/index.html",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "ui5 build preload --clean-dest --config ui5-deploy.yaml --include-task=generateManifestBundle generateCachebusterInfo && rimraf archive.zip"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@sap/ux-ui5-tooling": "^1.1.7",
        "@ui5/cli": "^2.10.2",
        "rimraf": "3.0.2"
    },
    "ui5": {
        "dependencies": [
            "@sap/ux-ui5-tooling"
        ]
    }
}
